/* ============================================================
   TALISMAN — main.js
   Bootstrap. Wires nothing fancy yet — just shows the title screen.
   ============================================================ */
(function () {
  const T = window.TALISMAN;
  document.addEventListener("DOMContentLoaded", () => {
    T.UI.show("title");
  });
})();
